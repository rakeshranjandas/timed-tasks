export const workerUrl = new URL(
    "$lib/workers/tick-simple.worker.ts",
    import.meta.url
);

// export const workerUrl = new URL(
//     "$lib/workers/tick-ref-timestamp.worker.ts",
//     import.meta.url
// );
